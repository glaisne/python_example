pipeline {

    agent { 
        node { 
            label 'linux'
        }
    }
    stages {
        stage ("Information") {
            steps {
                echo 'Designing a python/Jenkins example setup'
            }
        }
        stage ("linting") {
            steps {
                pylint --rcfile .\pylint.config --reports y az
            }
        }
    }
}
